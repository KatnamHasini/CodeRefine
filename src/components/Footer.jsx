import React from 'react';

const Footer = () => {
    return (
        <footer className="footer">
            <p>&copy; {new Date().getFullYear()} CodeRefine. Built with ❤️ for developers.</p>
        </footer>
    );
};

export default Footer;
